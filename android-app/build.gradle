buildscript {
    repositories {
        maven { url = uri("${rootDir}/../.tools/local-maven") }
        google()
        mavenCentral()
    }
    dependencies {
        classpath "com.android.tools.build:gradle:8.13.0"
    }
}
