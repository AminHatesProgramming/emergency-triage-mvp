<!-- rtl-normalized -->
<div dir="rtl" align="right">

# چک‌لیست آزمون APK نهایی روی دستگاه واقعی

## شناسنامه artifact

| مورد | مقدار |
|---|---|
| فایل | `Emdadyar-1.0.0-release.apk` |
| شناسه بسته | `ir.pourmand.emdadyar` |
| نسخه | `1.0.0` (`versionCode=1`) |
| حداقل Android | Android 6 / API 23 |
| SHA-256 | `1E4FC0C64CB79EA561F9E069D37841759A5E34806734A119A1F1E350FB38FE7E` |

پیش از نصب، hash فایل روی رایانه با `docs/market/release/SHA256SUMS.txt` تطبیق داده شود. نتیجه این چک‌لیست فقط برای همین hash معتبر است.

## مشخصات دستگاه

| فیلد | مقدار ثبت‌شده |
|---|---|
| سازنده و مدل | نیاز به تکمیل دارد |
| نسخه Android | نیاز به تکمیل دارد |
| اندازه/رزولوشن صفحه | نیاز به تکمیل دارد |
| تاریخ آزمون | نیاز به تکمیل دارد |
| آزمون‌کننده | نیاز به تکمیل دارد |

## کنترل نصب و اجرا

- [ ] APK بدون خطای امضا نصب می‌شود.
- [ ] نام و آیکن «امداد یار» در فهرست برنامه‌ها درست است.
- [ ] برنامه بدون اینترنت باز می‌شود و فرم قابل استفاده است.
- [ ] محتوای بالای صفحه زیر ساعت، اعلان و درصد شارژ قرار نمی‌گیرد.
- [ ] هیچ دکمه یا تب «نصب» داخل نسخه APK دیده نمی‌شود.
- [ ] تب «راهنما» در بالا و نوار پایین باز می‌شود.
- [ ] نوار پایین روی دکمه‌ها یا فیلدها هم‌پوشانی ندارد.

## سناریوهای عملکردی

| سناریو | انتظار | نتیجه واقعی |
|---|---|---|
| فرم خالی | درخواست ثبت شکایت اصلی یا یک علامت حیاتی؛ بدون برآورد ریسک | نیاز به تکمیل دارد |
| بیمار پرخطر | نمایش رسیدگی فوری، هشدارهای مهم و اقدام بعدی | نیاز به تکمیل دارد |
| اطلاعات کم | ارزیابی اولیه همراه با کامل بودن داده و فیلدهای پیشنهادی | نیاز به تکمیل دارد |
| ضربان ۱۵۰ در بزرگسال | فعال شدن فوریت و قانون ایمنی بدون تغییر احتمال خام مدل | نیاز به تکمیل دارد |
| بیمار با علائم عادی | نبود ارتقای ایمنی بی‌دلیل | نیاز به تکمیل دارد |
| پاک کردن فرم | حذف سناریوی فعال و بازگشت نتیجه به حالت اولیه | نیاز به تکمیل دارد |
| کپی خلاصه | کپی متن خلاصه همراه با تذکر پشتیبان تصمیم | نیاز به تکمیل دارد |
| خروج و اجرای دوباره | اجرای سالم برنامه و عدم ذخیره پیش‌فرض داده بیمار | نیاز به تکمیل دارد |

## شواهد لازم

1. تصویر صفحه اصلی که فاصله نوار وضعیت و نبود کنترل نصب را نشان دهد: `android-final-home.png`
2. تصویر نتیجه بیمار پرخطر با هشدار و اقدام بعدی: `android-final-critical.png`
3. hash محاسبه‌شده فایل و مشخصات دستگاه در همین سند یا QA Log ثبت شود.
4. نتیجه در Jira `EMD-60` و Notion QA Test Log به‌روزرسانی شود.

## معیار پایان

آزمون زمانی کامل است که همه کنترل‌های نصب و رابط عبور کنند، سناریوهای عملکردی نتیجه مورد انتظار بدهند و دو تصویر از همین artifact ثبت شوند. این آزمون، اعتبارسنجی بالینی محصول محسوب نمی‌شود.

</div>
